# Jenkins Configuration
JENKINS_URL=http://localhost:8080
JENKINS_USER=ci-user
JENKINS_API_TOKEN=your-token-here  # Or use jenkins-ci-user-token.json

# Job Configuration
JOB_NAME=DevSecOps-Medical-Record-Pipeline

# Credentials to inject into Jenkins (via Groovy init or environment)
# These are used by jenkins-init/create_credentials.groovy if running the container with these vars
DOCKER_USERNAME=myuser
DOCKER_PASSWORD=mypassword

ARGOCD_USERNAME=admin
ARGOCD_PASSWORD=myargopassword
